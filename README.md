Spring application for database access.

Sample table created in MariaDB-
CREATE TABLE Employee(ID INT NOT NULL AUTO_INCREMENT,FIRST_NAME VARCHAR(100) NOT NULL, LAST_NAME VARCHAR(100) NOT NULL, PRIMARY KEY (ID));